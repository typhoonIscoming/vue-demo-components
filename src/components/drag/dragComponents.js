import {
    VChart, VLine, VArea, VBar, VPie, VPoint, VScale, VAxis,
    VGuide, VTooltip, VLegend } from 'vux'

export default {
    esLetComponent: () => import('./esLet'),
    VChart,
    VLine,
    VArea,
    VBar,
    VPie,
    VPoint,
    VScale,
    VAxis,
    VGuide,
    VTooltip,
    VLegend,
}
