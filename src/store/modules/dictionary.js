
const initState = {
    list: [],
}


const getters = {
    getList: state => state.list,
}

const actions = {}

const mutations = {}

export default {
    state: initState,
    getters,
    actions,
    mutations,
    namespaced: true,
}
